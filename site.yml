---
# This playbook attempts to install Tacker

- name: install tacker on target server 
  hosts: tacker-server
  remote_user: root

  roles:
    - server-install

- name: install tacker database
  hosts: database
  remote_user: root

  roles:
    -  database-install

- name: install horizon plugin on controllers
  hosts: controller
  remote_user: root

  roles:
    - horizon-install

- name: configure tacker database
  host: database
  remote_user: root

  roles:
    - database-configure

- name: configure and start tacker server
  host: tacker-server
  remote-user: root

  roles:
    - server-configure
